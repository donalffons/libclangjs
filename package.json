{
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "upload-build-cache": "mkdir -p node_modules/.cache/turbo && gsutil -m rsync -d -r node_modules/.cache/turbo gs://libclangjs-turborepo-remote-cache",
    "download-build-cache": "mkdir -p node_modules/.cache/turbo && gsutil -m rsync -d -r gs://libclangjs-turborepo-remote-cache node_modules/.cache/turbo"
  },
  "packageManager": "pnpm@7.25.1",
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.7.0"
  }
}